# Быстрый старт деплоя

## 🚀 Быстрая инструкция (5 минут)

### На вашем компьютере:

1. **Соберите проект:**
   ```bash
   npm install
   npm run build
   ```

2. **Создайте .env.production:**
   ```env
   VITE_API_URL=https://yourdomain.com/api
   ```

3. **Загрузите dist/ на сервер:**
   ```bash
   scp -r dist/* root@your-server-ip:/var/www/frontend/dist/
   ```

### На VPS сервере:

1. **Установите Node.js:**
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
   apt install -y nodejs
   ```

2. **Установите Nginx:**
   ```bash
   apt install -y nginx
   ```

3. **Установите PM2:**
   ```bash
   npm install -g pm2
   ```

4. **Запустите бэкенд:**
   ```bash
   cd /var/www/backend
   pm2 start npm --name "bonfire-api" -- start
   pm2 save
   ```

5. **Настройте Nginx** (см. полный гайд в DEPLOY.md)

6. **Получите SSL сертификат:**
   ```bash
   apt install -y certbot python3-certbot-nginx
   certbot --nginx -d yourdomain.com
   ```

## 📖 Полная инструкция

См. файл **DEPLOY.md** для подробной инструкции.

## ⚡ Автоматический скрипт

Используйте `deploy.sh` для автоматической сборки:

```bash
./deploy.sh production
```

---

**Важно:** Убедитесь, что:
- ✅ Бэкенд запущен на порту 3000
- ✅ Nginx настроен и работает
- ✅ Домен указывает на IP сервера (если используется домен)
- ✅ Файрвол разрешает порты 80 и 443

