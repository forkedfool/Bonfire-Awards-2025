# 🔍 Диагностика проблемы с дефолтной страницей Nginx

## Проблема
После настройки конфигурации в браузере всё ещё показывается дефолтная страница Nginx.

## Пошаговая диагностика

### 1. Проверьте активные конфигурации

```bash
# Посмотрите все активные конфигурации
ls -la /etc/nginx/sites-enabled/

# Должна быть только ссылка на bonfire-awards
# Если есть default - удалите её:
sudo rm /etc/nginx/sites-enabled/default
```

### 2. Проверьте содержимое конфигурации

```bash
# Проверьте, что конфигурация создана правильно
sudo cat /etc/nginx/sites-available/bonfire-awards

# Убедитесь, что server_name правильный
# Если используете IP - раскомментируйте строку с IP
# Если используете домен - укажите правильный домен
```

### 3. Проверьте, что файлы фронтенда на месте

```bash
# Проверьте существование директории dist
ls -la /var/www/frontend/dist/

# Должен быть файл index.html
ls -la /var/www/frontend/dist/index.html

# Если файлов нет - нужно собрать проект:
cd /var/www/frontend
npm run build
```

### 4. Проверьте права доступа

```bash
# Установите правильные права
sudo chown -R www-data:www-data /var/www/frontend/dist
sudo chmod -R 755 /var/www/frontend/dist
```

### 5. Проверьте конфигурацию Nginx

```bash
# Проверка синтаксиса
sudo nginx -t

# Если есть ошибки - исправьте их
```

### 6. Перезагрузите Nginx

```bash
# Полная перезагрузка (не reload)
sudo systemctl restart nginx

# Проверьте статус
sudo systemctl status nginx
```

### 7. Проверьте логи

```bash
# Логи ошибок
sudo tail -f /var/log/nginx/error.log

# Логи доступа
sudo tail -f /var/log/nginx/bonfire-access.log
```

### 8. Проверьте, какой server_name используется

Если вы обращаетесь по IP адресу, но в конфигурации указан домен (или наоборот), Nginx не будет использовать эту конфигурацию.

**Решение:** Используйте `server_name _;` для приема всех запросов:

```nginx
server {
    listen 80;
    server_name _;  # Принимает все запросы
    
    # ... остальная конфигурация
}
```

Или укажите конкретный IP:

```nginx
server {
    listen 80;
    server_name 102.308.8.xxx;  # Ваш IP адрес
    
    # ... остальная конфигурация
}
```

## Быстрое исправление

Выполните все команды по порядку:

```bash
# 1. Удалите дефолтную конфигурацию
sudo rm -f /etc/nginx/sites-enabled/default

# 2. Проверьте активные конфигурации
ls -la /etc/nginx/sites-enabled/

# 3. Отредактируйте конфигурацию, чтобы принимать все запросы
sudo nano /etc/nginx/sites-available/bonfire-awards
```

Измените строку `server_name` на:

```nginx
server_name _;
```

Или на ваш IP адрес.

```bash
# 4. Проверьте наличие файлов
ls -la /var/www/frontend/dist/index.html

# 5. Если файлов нет - соберите проект
cd /var/www/frontend
npm install
npm run build

# 6. Установите права
sudo chown -R www-data:www-data /var/www/frontend/dist

# 7. Проверьте конфигурацию
sudo nginx -t

# 8. Перезапустите Nginx
sudo systemctl restart nginx

# 9. Проверьте статус
sudo systemctl status nginx
```

## Проверка в браузере

После выполнения команд:

1. Очистите кэш браузера (Ctrl+Shift+Delete)
2. Откройте сайт в режиме инкогнито
3. Или используйте curl для проверки:

```bash
curl -I http://your-server-ip
```

Должен вернуться HTTP 200, а не дефолтная страница Nginx.

## Альтернативное решение: Использовать default_server

Если ничего не помогает, сделайте bonfire-awards конфигурацией по умолчанию:

```nginx
server {
    listen 80 default_server;  # Добавьте default_server
    server_name _;
    
    # ... остальная конфигурация
}
```

Затем:

```bash
sudo nginx -t
sudo systemctl restart nginx
```

---

**После выполнения этих шагов дефолтная страница должна исчезнуть!** ✅

